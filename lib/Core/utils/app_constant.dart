import 'package:flutter/material.dart';

class AppConstant {
  static const Color kPrimaryColor = Color(0xff100B20);
  static const kDuration = Duration(milliseconds: 250);
  static const KPaddingHome = EdgeInsets.symmetric(horizontal: 30);
}
